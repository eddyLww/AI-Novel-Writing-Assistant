已为你定制好专属的部署文档（已填入你的腾讯云 IP `43.139.102.153` 与域名 `aixs.eddy.ink`）：

---

# 🚀 腾讯云 + 宝塔 Docker 部署文档（专属定制版）

### 📌 服务器信息
- **服务器 IP**：`43.139.102.153`
- **解析域名**：`aixs.eddy.ink` （请确保该域名在 DNS 解析中已添加 A 记录指向 `43.139.102.153`）
- **腾讯云安全组需放行端口**：`80` (HTTP), `443` (HTTPS), `8888` (宝塔面板端口)

---

## 第一步：宝塔安装 Docker 插件

1. 登录宝塔面板，在左侧菜单栏点击 **Docker**。
2. 若未安装，直接点击提示中的 **“立即安装”**。

---

## 第二步：上传项目与配置环境变量

1. 打开宝塔面板左侧的 **文件** -> 进入目录 `/www/wwwroot/`。
2. 新建文件夹：`ai-novel-docker`。
3. 将项目源码上传并解压到 `/www/wwwroot/ai-novel-docker/` 目录下。
4. 进入 `/www/wwwroot/ai-novel-docker/server/` 目录，新建或编辑 `.env` 文件，贴入配置：

```env
PORT=3000
HOST=0.0.0.0
CORS_ORIGIN=http://aixs.eddy.ink,https://aixs.eddy.ink,http://43.139.102.153

# 大模型 API 配置（已验证调通的中转站配置）
OPENAI_API_KEY=sk-HuSRfmwdgpnDVu4vQk9RW34hAimH8Ec4W1ISZpCsKCud8pMn
OPENAI_BASE_URL=https://www.177911.com/v1
OPENAI_MODEL=gpt-5.6-sol
```

---

## 第三步：创建 Docker-Compose 配置文件

在 `/www/wwwroot/ai-novel-docker/` 根目录下，新建文件 `docker-compose.yml`，贴入以下完整内容：

```yaml
version: '3.8'

services:
  # ----------------------------------------------------
  # 1. 后端 API 服务配置 (服务名为 api)
  # ----------------------------------------------------
  api:
    build:
      context: .             # 指定构建上下文为项目根目录
      dockerfile: Dockerfile.api  # 告诉 Docker 使用根目录下的 Dockerfile.api 进行构建
    container_name: ai-novel-api   # 启动后的容器名称
    restart: always                # 崩溃或服务器重启时，自动挂起重启容器
    ports:
      - "3000:3000"                # 端口映射：[宿主机端口 : 容器内端口]
    volumes:
      # 文件挂载：把服务器上的 .env 配置文件映射到容器内部，方便随时修改密钥
      - ./server/.env:/app/server/.env
      # 目录挂载：把生产的数据库/图片存到服务器本地磁盘，容器删掉数据也不丢失
      - ./storage:/app/storage
    environment:
      - NODE_ENV=production        # 告诉 Node.js 以生产环境性能模式运行

  # ----------------------------------------------------
  # 2. 前端 Nginx 网页服务配置 (服务名为 web)
  # ----------------------------------------------------
  web:
    build:
      context: .
      dockerfile: Dockerfile.web   # 使用根目录下的 Dockerfile.web 打包前端静态资源与 Nginx
      args:
        # 构建参数：编译前端 React 代码时，把请求接口的根地址写入前端包中
        - VITE_API_BASE_URL=http://aixs.eddy.ink/api
    container_name: ai-novel-web
    restart: always
    ports:
      - "8881:8881"                # 映射宿主机的 8881 端口提供网页访问
    depends_on:
      - api                        # 依赖关系：确保 api 容器启动成功后，再启动 web 前端
```

---

## 第四步：启动 Docker 容器构建

打开宝塔面板的 **终端**（或使用 SSH 连接服务器），复制运行以下命令：

```bash
# 1. 进入项目 Docker 目录
cd /www/wwwroot/ai-novel-docker

# 2. 一键构建并后台启动容器
docker-compose up -d --build
```

> **检查运行状态**：运行 `docker ps`，看到 `ai-novel-api` 和 `ai-novel-web` 状态均为 `Up` 即可。

---

## 第五步：在宝塔配置 `aixs.eddy.ink` 域名绑定与反向代理

1. 进入宝塔面板 -> 点击 **网站** -> **添加站点**：
   - **域名**：`aixs.eddy.ink` （如需 IP 访问可加上 `43.139.102.153`）
   - **根目录**：使用默认目录即可
   - **程序类型**：纯静态
2. 点击 **提交** 创建网站。
3. 点击刚创建的网站设置 -> **配置文件** (Nginx 配置文件)，覆盖替换为以下完整 Nginx 规则：

```nginx
server {
    listen 80;
    listen 443 ssl http2 ;
    server_name aixs.eddy.ink 43.139.102.153;

    # 重点：放行宝塔 SSL 文件验证路径
    location ^~ /.well-known/ {
        root /www/wwwroot/aixs.eddy.ink;
    }

    # 1. 前端页面代理到 Docker 8881 端口
    location / {
        proxy_pass http://127.0.0.1:8881;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # 2. 后端 API 接口代理到 Docker 3000 端口
    location /api/ {
        proxy_pass http://127.0.0.1:3000/api/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        
        # 延长超时限制以支持长文本 AI 生成
        proxy_connect_timeout 300s;
        proxy_read_timeout 300s;
        proxy_send_timeout 300s;
    }
}

```
4. 点击 **保存**。如果申请了 SSL 证书，可以在宝塔面板的 **SSL 证书** 选项中为 `aixs.eddy.ink` 一键申请开启 Let's Encrypt 免费证书。

---

## 🌐 部署完成与访问

在浏览器中输入域名：
👉 **http://aixs.eddy.ink** (或 **http://43.139.102.153**)

访问即可直接进入已经与中转站 `gpt-5.6-sol` 调通的 **AI 小说创作工作台**！