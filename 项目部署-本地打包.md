Edited %E6%8A%BD%E5%8F%96%E4%B9%A6%E4%B8%AD%E6%A1%88%E4%BE%8B%E7%B4%A0%E6%9D%90Skill.md
Edited SKILL.md

非常理解！在配置比较低的服务器（如 2核4G 或 2核2G）上，使用 Docker 在线编译 TypeScript (`tsc`) 会占用 **100% 的 CPU 并持续好几分钟**，导致服务器卡死。

解决这个问题的**黄金方案是“本地编译打包 + 服务器只运行 Node.js 镜像”**（即：把耗费 CPU 的编译工作放在你本地电脑上做，服务器只负责轻量运行编译好的 JavaScript）。

以下为你提供最推荐的 **本地编译打包部署方案**：

---

### 💡 终极解法：本地打包镜像，服务器一键拉取运行

#### 第一步：在你的本地电脑（Windows CMD/终端）完成打包

在你的本地电脑上打开项目根目录：

```bash
# 1. 在本地电脑安装依赖并编译共享库和后端
pnpm install
pnpm --filter @ai-novel/shared build
pnpm --filter @ai-novel/server prisma:generate
pnpm --filter @ai-novel/server build
pnpm --filter @ai-novel/client build
```
*(在本地电脑拥有强劲 CPU 的情况下，这一步只需要十几秒)*。

---

#### 第二步：修改 `docker-compose.yml` 使用轻量化运行模式

将项目根目录下的 `docker-compose.yml` 修改为以下内容，**不再让服务器执行复杂的 `build`**：

```yaml
version: '3.8'

services:
  # 1. 后端 API 服务
  api:
    image: node:20-bookworm-slim
    container_name: ai-novel-api
    restart: always
    working_dir: /app
    ports:
      - "3000:3000"
    volumes:
      - ./:/app
      - ./server/.env:/app/server/.env
      - ./storage:/app/storage
    command: sh -c "node ./server/dist/app.js"
    environment:
      - NODE_ENV=production
      - NODE_PATH=/app/node_modules:/app/server/node_modules

  # 2. 前端 Nginx 网页服务
  web:
    image: nginx:alpine
    container_name: ai-novel-web
    restart: always
    ports:
      - "8881:80"
    volumes:
      - ./client/dist:/usr/share/nginx/html
      - ./infra/nginx/ai-novel-web.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - api

```

---

#### 第三步：上传至服务器并瞬间启动

1. 把在本地电脑编译好的压缩包（包含 `node_modules`、`server/dist`、`client/dist`、`shared/dist`）上传到宝塔服务器。
2. 在宝塔终端运行：
   ```bash
   cd /www/wwwroot/ai-novel-docker
   docker-compose up -d
   ```

> **效果对比**：
> - **原方案（服务器编译）**：占用 100% CPU，耗时 30 分钟甚至卡死溢出。
> - **新方案（只运行容器）**：占用不到 5% CPU，耗时 **3 秒钟**瞬间启动完成！可以完全拯救你的服务器。





# 使用本地 docker 打包成tar
**非常聪明的选择！本地电脑安装 Docker Desktop 是解决所有项目部署问题最彻底、最专业的方式！**

只要本地电脑装了 Docker，在本地打包镜像（镜像里会自动包含所有 Linux 依赖和编译好的代码），然后打包上传到服务器一键加载，服务器端 **0 编译、0 CPU 占用、0 依赖缺失风险**！

---

### 💻 本地电脑安装 Docker Desktop 教程 (Windows)

#### 1. 下载 Docker Desktop
- 官方下载链接：[Docker Desktop for Windows Official](https://desktop.docker.com/win/main/amd64/Docker%20Desktop%20Installer.exe)
- 点击下载并安装，安装时勾选默认选项（如 WSL 2 选项），安装完成后提示重启电脑。

#### 2. 重启后启动 Docker Desktop
重启电脑后打开桌面上的 **Docker Desktop** 图标，等待状态显示绿色的 **Engine running**，即代表本地 Docker 安装成功。

---

### 🚀 安装好后，在本地项目目录一键打包上传

在本地项目根目录 `D:\00自媒体创业\AI写作\AI-Novel-Writing-Assistant` 打开终端运行以下 **3 条命令**：

```cmd
# 1. 本地打包后端镜像
docker build -t ai-novel-api:latest -f Dockerfile.api .

# 2. 本地打包前端镜像
docker build -t ai-novel-web:latest --build-arg VITE_API_BASE_URL=http://aixs.eddy.ink/api -f Dockerfile.web .

# 3. 将两个打好的镜像打包成一个文件
docker save -o ai-novel-images.tar ai-novel-api:latest ai-novel-web:latest
```

---

### 🌐 服务器端 3 秒极速上线

将生成的 `ai-novel-images.tar` 文件通过宝塔上传到服务器 `/www/wwwroot/ai-novel-docker/`。

服务器上的 `docker-compose.yml` 还原为标准的 Compose 文件：
```yaml
version: '3.8'

services:
  api:
    image: ai-novel-api:latest
    container_name: ai-novel-api
    restart: always
    ports:
      - "3000:3000"
    volumes:
      - ./server/.env:/app/server/.env
      - ./storage:/app/storage

  web:
    image: ai-novel-web:latest
    container_name: ai-novel-web
    restart: always
    ports:
      - "8881:8080"
    depends_on:
      - api
```

在服务器终端只需运行 2 行命令：
```bash
cd /www/wwwroot/ai-novel-docker
docker load -i ai-novel-images.tar
docker-compose up -d
```

瞬间部署成功，服务器负载接近为 0，并且绝不会再报任何 `MODULE_NOT_FOUND`！
