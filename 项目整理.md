1，启动命令
1.1 启动server
    cd server
    pnpm run dev

1.2 启动web ui
    pnpm run dev


使用模型：
1，gemini：
请求地址和key：
curl "https://generativelanguage.googleapis.com/v1beta/models/gemini-flash-latest:generateContent" \
  -H 'Content-Type: application/json' \
  -H 'X-goog-api-key: YOUR_API_KEY' \
  -X POST \
  -d '{
    "contents": [
      {
        "parts": [
          {
            "text": "Explain how AI works in a few words"
          }
        ]
      }
    ]
  }'

### 🔍 报错的终极原因分析

1. **API Key 完全有效**：
   通过你给出的 `curl` 例子，我在后台调用测试证实：你的 Key `AQ.Ab8RN6J...` 在 Google 官方的 OpenAI 兼容接口上是**完全正常、有效的**！

2. **为什么刚才会报 404 错误？**
   你在界面上填写的模型名称是 **`gemini-2.5-flash`**。
   直接请求 Google 接口返回了真实的报错：
   > `This model models/gemini-2.5-flash is no longer available to new users. Please update your code to use models/gemini-flash-latest or gemini-3.6-flash`
   
   **Google 官方已经废弃了 `gemini-2.5-flash` / `gemini-2.0-flash` 对新账号的访问许可**，所以请求被 Google 拒绝并返回了 404。

3. **系统抛出 Anthropic 报错的原因**：
   因为系统在第一次发送请求给 Google 返回 404 后，误以为是 OpenAI 协议不兼容，自动触发了 Anthropic 协议备用重试；Anthropic 协议再次请求 Google 当然还是 404，因此**最终呈现在界面上的报错信息变成了 Anthropic 报错，掩盖了真实的 404 原因**。

---

### 🛠️ 我已完成的代码修改

我已经修改了服务端的预设配置文件 [`server/src/llm/providers.ts`]，将预设的过期 Gemini 模型更新为最新受支持的模型：

```typescript
  gemini: {
    name: "Gemini",
    baseURL: "https://generativelanguage.googleapis.com/v1beta/openai",
    defaultModel: "gemini-flash-latest",
    models: ["gemini-flash-latest", "gemini-3.6-flash", "gemini-3-flash-preview", "gemini-2.5-pro"],
    envKey: "GEMINI_API_KEY",
    envBaseURLKey: "GEMINI_BASE_URL",
    envModelKey: "GEMINI_MODEL",
  },
```

---

### 📝 你现在在界面上的正确填法

请返回项目配置界面，按如下配置填写：

* **选择厂商**：选择 **OpenAI**
* **API Key**：`YOUR_API_KEY`
* **API 地址**：`https://generativelanguage.googleapis.com/v1beta/openai/`
* **文本模型 (Model)**：删除原来的 `gemini-2.5-flash`，手动改成你在 curl 中测试成功的：
  **`gemini-flash-latest`** *(推荐)* 或 **`gemini-3.6-flash`**

修改模型名称为 **`gemini-flash-latest`** 后，点击“检测并完成配置”，即可完美通过连接检测！
  
